Ceci est la traduction du code source depuis lequel est généré
http://fr.eloquentjavascript.net

Pour le générer vous-même, installez GHC (avec les bibliothèques
parsec2, mtl et utf8-string), et exécutez `make.sh` dans le
répertoire `renderer/`. Vous devriez obtenir un fichier binaire
`render` que vous pourrez exécuter sur un fichier texte afin de
produire les fichiers HTML du livre.

Le fichier `book` contient les sources pour le vrai livre. Son
format n'est absolument pas documenté, mais relativement simple.
